DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=gestar
SECRET_KEY=change-me
FRONTEND_ORIGIN=http://localhost:5173

# En producción (Railway, etc.) alcanza con setear estas dos en vez de las
# DB_* de arriba, y no hace falta tocar el resto:
# DATABASE_URL=mysql://user:password@host:port/dbname   (Railway la inyecta sola)
# FLASK_ENV=production
